install:
  - ps: .\scripts\appveyor.ps1

platform:
  - x86
  - x64

environment:
  matrix:
    - RUST_VERSION: 1.1.0
    - RUST_VERSION: beta
    - RUST_VERSION: nightly

build: false

test_script:
  - echo no testing done
